<section id="admin-email" class="container content-section" style="text-align:justify">
  <div class="form-group">
    <label>Od:</label>
    {{input  class="form-control" value=sender}}
    (e-mail fyzické osoby, ne semináře, aby na něj mohly přijít zprávy o nedoručitelnosti či jiných problémech)
  </div>
  <div class="form-group">
    <label>Reply-To:</label>
    {{input  class="form-control" value=replyTo}}
    (sem by měli řešitelé případně odepisovat)
  </div>
  <div class="form-group">
    <label>Komu:</label>
    <select {{action "to_select"}} class="form-control" id="to_sel" multiple="multiple">
      {{#each years as |t|}}
          <option value="{{t.id}}">{{t.year}}</option>
      {{/each}}
    </select>
    (e-mail bude odeslán řešitelům vybraných ročníků)
  </div>
  <div class="form-group">
    <label>Pohlaví:</label>
    {{#x-select value=gender class="form-control"}}
      {{#x-option value="both"}}Všichni{{/x-option}}
      {{#x-option value="male"}}Chlapci{{/x-option}}
      {{#x-option value="female"}}Dívky{{/x-option}}
    {{/x-select}}
  </div>
  <div class="form-group">
    <label>Slepá kopie:</label>
    <textarea class="form-control" style="min-width: 100%" rows="3" id="bcc"></textarea>
    (e-maily oddělujte čárkou anebo středníkem)
  </div>
  <div class="form-group">
    <label>Předmět:</label>
    {{input  class="form-control" value=subject}}
  </div>
  <div class="form-group">
    <label>Obsah:</label>
    {{text-editor value=text}}
  </div>
  <div class="form-group">
    {{input type="checkbox" checked=karlikSign}}
    <label>Připojit na konec zprávy Karlíkův podpis</label><br>
    {{input type="checkbox" checked=easteregg}}
    <label>Připojit do PS easteregg</label>
  </div>
  <div class="row" style="margin-bottom:20px; margin-left:0px">
    <button class="btn btn-primary inline" id="send-email-button" {{action "email"}}>Odeslat</button>
    {{#if send_status}}
      <div class="alert alert-info" style="margin-top:20px">{{send_status}}</div>
    {{/if}}
  </div>
</section>
